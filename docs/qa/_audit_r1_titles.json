{
  "confirmedTitles": [
    "Insight-dismiss write escapes the /api/finance admin gate (role bypass)",
    "Variation create/edit uses bare fetch() — no auth token, every save 401s",
    "WIPAA panel calls a renamed/nonexistent endpoint — 404, panel never populates",
    "KPI mismatch: 'Claims paid' is inc-GST cash but 'Claims issued' is ex-GST — paid shows ~110% of issued",
    "Payment status uses hardcoded *1.1 GST and contradicts how payment_amount is read everywhere else (ex-GST vs inc-GST)",
    "Payment can be recorded against a draft (never-issued) claim",
    "Void and Dispute have no status precondition — a paid claim can be voided, a void claim re-disputed",
    "Variation sign is a non-atomic check-then-write → concurrent double-sign double-counts variation_amount in normalized_costs",
    "AI job-matching tier references undefined identifier MODEL — every AI-tier match silently fails",
    "Common estimate category names map to no workforce task → labour actuals silently $0",
    "Two budget lines mapping to the same workforce_task_category double-count labour actuals",
    "/budget, /summary and /closeout compute labour actuals from different cost bases and scopes — same job shows different numbers",
    "Budget re-seed upsert key (cost_type,category_name) diverges from DB UNIQUE(job_id,category_name) → 500 on cost_type flip",
    "Labour booked under timesheet task_category 'other' is dropped from per-category budget actuals",
    "Auto-layout adds calendar days, not working days — milestones land on weekends despite 'working days' model",
    "Frame-delivery anchor earlier than commencement produces a non-monotonic, nonsensical schedule",
    "Zero-advance terminal milestones (Practical completion / handover) collide on the same date as their predecessor",
    "Estimate-Items per-category SELL ex-GST understates supplier-direct / PC-PS lines (budget != client total)",
    "Legacy report XLSX import and API fetch set quoted_value to COST (net_total), silently dropping markup",
    "quoted_cost never populated from the XLSX import, so margin display is blank/wrong",
    "GST hardcoded as *1.1 in parser (report + API + PDF paths) — violates GST_RATE law and drifts from constants.js",
    "Worker PWA leaks supervisor/QC tasks — task_audience is never filtered server-side (D3 regression)",
    "site_tasks write/read endpoints leak raw Postgres errors and bypass apiResponse/camelCase laws",
    "Labour endpoint leaks per-worker and per-category labour cost ($) to non-director roles",
    "All /api/workforce timesheet read endpoints are requireAuth-only — any employee can enumerate the whole workforce's timesheets",
    "Worker log-hours job picker is unscoped — leaks full project/carpentry portfolio + client names to any magic-link holder and allows hour attribution to arbitrary jobs",
    "Overtime/double-time computed PER TASK-ENTRY, not per day — multi-task days are systematically underpaid and labour actuals under-booked",
    "mass-fill always INSERTs entries without clearing existing ones — re-running (or filling a day already logged) duplicates hours and doubles labour cost",
    "No rate snapshot: cost is (re)computed from the CURRENT pay rate on every approval, so a later rate change retroactively rewrites the cost of any approved-but-not-yet-completed timesheet",
    "Leading-hand completion photos stored as raw base64 in the DB — violates the migration-099 path-only design",
    "Timesheet 'Site' picker lists every project + carpentry job to any worker, and the POST accepts any project id (unscoped)",
    "Ripple cascade has no cycle guard — cyclic dependencies cause a runaway loop (client) / server crash (server)",
    "DB cascade + ripple-preview + reminders ignore typed task_dependencies (SS/FF/lag) — Sprint-3 typed deps are dead on the apply path",
    "Procurement lead-time notifications hardcode 3 weeks for AI-generated tasks (lead_time_weeks null, real value in lead_time_days)",
    "EOT apply shifts EVERY task (incl. completed/past) by approved days, with no idempotency guard",
    "Schedule PATCH (and EOT/baseline/ripple handlers) leak raw Postgres error strings to the browser and bypass ok()/err()",
    "Idempotency guard ignores trade — a sub covering 2 trades only gets the FIRST RFQ; the rest are silently skipped",
    "Role-bypass: /api/po/issue and the entire /api/rfq + /api/rfq-packages surface are requireAuth-only (no requireRole) despite the code comment claiming admin+supervisor",
    "RFQ package silently lost if /api/rfq-packages create returns a non-OK JSON error after all RFQs were emailed",
    "Issue-PO never stamps ordered_at → supplier lead-time learning loop silently never fires",
    "PO drafted/issued off cost_allowance alone contributes $0 to committed cost (Finance under-reports)",
    "draft-po has no existing-PO guard → re-drafting orphans the prior purchase_orders row",
    "Buildexact API estimate path drops the builder's margin → fee proposal underpriced (markup=0, cost×1.1)",
    "Hardcoded *1.1 GST in fee-proposal summary builder (law violation + understates price in fallback)",
    "Cost Intelligence routes leak raw Postgres/error strings to the client",
    "Headline TOTAL_INC_GST is a free-text override with no reconciliation against the category summary",
    "convert-to-job address-dedup overwrites another party's canonical client facts (cross-lead contamination)",
    "leads.stage has no DB CHECK constraint and all code hardcodes stage strings instead of LEAD_STAGES — silent pipeline drift",
    "Bulk email send + contact PII are reachable by any non-admin staff token (no requireRole on /api/crm)",
    "Smart-list sends ignore unsubscribe/consent entirely — unsubscribed contacts get re-emailed",
    "Link-unsubscribe is per-list only; no global suppression — contact still emailed via other lists",
    "increment_send_stat double-counts on Resend webhook retries (no event-id idempotency)",
    "All CRM created_by / added_by audit fields silently stored NULL (req.user never set)",
    "Final Assembly + Re-export run ffmpeg on a Supabase storage path as if it were a local file (and never download it)",
    "Streamed videos (/upload-video) are never persisted to storage — storage_path stays null and the temp source is deleted, so export/assembly is impossible",
    "Media consent (consent_for_marketing) is not enforced on AI content generation, streaming, preload preview, or video re-export — only on final /assemble",
    "Intelligence dashboard AI summary uses a stale/legacy model id and fails silently, so the summary never renders",
    "Meta access token sent in the URL query string for Graph API insight fetches",
    "Schedule write routes are requireAuth-only — an 'employee' (UI: schedule view-only) can create/edit/delete any project's schedule tasks",
    "Public unauthenticated induction submit has 100MB body limit and no rate limiting — DoS / storage-exhaustion / data-injection",
    "Operations trade/PO routes leak subcontractor PII to lowest-trust 'employee' role (no role gate)",
    "syncProjectSwms deletes all SWMS links before re-inserting (non-atomic) — a failed insert leaves the public induction with zero SWMS to acknowledge",
    "Uploaded compliance documents with no expiry date always render as 'missing' (red/Action Required)",
    "Carpentry job/summary/budget/costs API leaks $ (cost & margin) to cost-stripped supervisors — UI strip only, server sends everything",
    "FieldTasks/FieldDiary/FieldWHS load has no catch — a rejected Supabase fetch leaves the loading spinner stuck forever",
    "Editing template content in the Hub silently reverts admin-edited metadata (status/title/purpose/category/module) to catalog defaults",
    "Client can view internal/defect site photos — v2 media route and Home feed bypass the migration-110 client_visible flag",
    "Selection sign-off allows non-contractual invitees (architect/accountant) to make a binding, cost-impacting selection",
    "Diary→Journey seed INSERT throws pre-migration-103 (no draft row is ever created), not merely orphaned",
    "\"Approved variation $\" is defined three inconsistent ways → client and staff see different contract totals",
    "Unauthenticated cron endpoints send emails to subcontractors (spam / reputation abuse)",
    "Unauthenticated company IMAP inbox reader",
    "Carpentry cost/budget endpoints leak dollar figures to the supervisor role (cost-stripped role bypass)",
    "Unauthenticated Dropbox upload/folder-creation routes (write to company file store)",
    "Unauthenticated AI re-extract endpoint mutates RFQ rows and burns paid Claude/Dropbox calls",
    "Unauthenticated Blueprint AI endpoints (paid-API abuse, no auth)",
    "Unauthenticated read of unmatched quote emails (PII / correspondence leak)",
    "Double-GST cost booking: Buildexact material push falls back to inc-GST amount_total as the ex-GST unit cost",
    "Actuals undercount: normalized-cost rollup sums only amount_ex_gst, dropping invoices that only carry amount_total"
  ],
  "lowTitles": [
    "Hardcoded *1.1 / *0.1 GST throughout finance code instead of GST_RATE/incGst/gstAmount (project law)",
    "Dead no-op branch in cursor update (`cursor = cursor`) signals a missed case",
    "Estimate-Items rows with a blank CategoryDescription are silently dropped",
    "Worker task category filter whitelist drifted from the expanded site_tasks.category CHECK (mig 114)",
    "Month calendar paints every working day red ('not logged') while the new month is still loading",
    "'Today' computed in UTC (toISOString) instead of business TZ — off-by-one on overdue/procurement near midnight",
    "Additional-send and win-finalize write rfqs.trade as a human label, which the RfqEngine resume path can fail to map back to a canonical slug",
    "win-finalize contract-value fallback hardcodes /1.1 instead of GST_RATE/exGst helper",
    "Issue button unreachable when item status is advanced past po_drafted, leaving a draft PO unissuable from the UI",
    "pretender_estimates.created_by always null — wrong req.user used instead of req.caller",
    "Transcript-apply writes qualify_* / estimated_value to lead but never stamps provenance; estimated_value applied as raw AI value",
    "Desktop Site Diary defaults entry_date in UTC, not AU-local — regression vs the AU-local ymd fix used elsewhere",
    "Incident-report PATCH cannot set in_progress and resolving is open to any staff member with no project scope check",
    "Meeting confirm/decline are not restricted to primary/secondary — architect/accountant invitees can respond on the client's behalf",
    "Legacy portal /budget mixes ex-GST contract+variations with inc-GST claim amounts in one view",
    "Unauthenticated subcontractor CSV→Google Sheet creation",
    "Auto-approve threshold compares an inc-GST total against an ex-GST limit"
  ]
}
