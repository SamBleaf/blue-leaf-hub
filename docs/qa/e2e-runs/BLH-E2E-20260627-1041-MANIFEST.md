# BLH E2E Walkthrough — Manifest

**Run ID:** `BLH-E2E-20260627-1041`
**Date:** 2026-06-27 (Adelaide)
**Operator:** Claude (E2E walkthrough / QA — no product code changes without separate Sam approval)
**Target:** `http://localhost:8787` (running dev server — per Sam, new code not fully deployed)
**Branch:** `portal-v2` (3 commits ahead of origin/main; 47 modified + 184 untracked working tree)

---

## Phase 0 — Preflight results

| Check | Result |
|-------|--------|
| `GET /api/health` | ✅ 200 (`{ok:true, model:claude-sonnet-4-5}`) |
| App loads in browser | ✅ SPA served at `/`, redirects to `/login` (Blue Leaf Hub) |
| Browser tooling available | ✅ Claude in Chrome — "Browser 1" (macOS, local) connected |
| Admin/staff login works | ✅ **PASSED** — Sam approved e2e-admin session injection (base64 round-trip into Supabase localStorage key `sb-khehclrwppjvrogyxmdb-auth-token`). Driving as **E2E Director** (admin). No password typed into the UI. |
| Mail transport test/sandbox? | ⚠️ **PRODUCTION** — Resend `re_QGi…` + real SMTP/IMAP (`mail.b…`) + Gmail `admin@…`. **No real RFQ sends** — Phase 6 simulated/gap-documented |
| External integrations (Buildxact/Dropbox) | ⚠️ 2 keys configured. **Avoid real side-effects** — convert→job folder, BX sync, PO issue, win-finalize get inspected/API-checked, not live-fired |
| Production data risk | Test data only, all run-ID tagged; cleanup planned |

**Preflight verdict:** PROCEED once Sam authenticates the browser. Two standing constraints recorded above (prod mail; external integrations) → the send/sync/external phases will be UI-inspected + safe-API-checked + gap-documented, never live-fired.

---

## Test dataset (realistic-but-fake, run-ID tagged)

- **Client:** Amelia Hartley · `amelia.hartley+BLH-E2E-20260627-1041@example.test` · 0412 555 019 · architect-referred private residential
- **Project:** `BLH E2E TEST — Norwood Alteration & Addition — BLH-E2E-20260627-1041` · 14 Jarrah Street, Norwood SA 5067 · ~$950,000 · rear extension + reno + ensuite + cladding/roof/windows · 32 weeks
- **Trades/quotes (ex GST):** Carpentry $86,500 · Concrete $74,250 · Windows $118,900 · Roofing $42,700 · Electrical $36,800 · Plumbing $44,200 (all `BLH E2E TEST … Co`)

---

## Records / files / artefacts created (filled as the run proceeds)

| Type | Name / ID | Phase | Cleanup action | Result |
|------|-----------|-------|----------------|--------|
| Lead | Amelia Hartley · `5367c278-32db-47fd-84ef-b4dd4ecae6cf` · email `amelia.hartley+BLH-E2E-20260627-1041@example.test` | 1–4 | delete via DB (email LIKE run-id) | ⏳ created; advanced to Fee Proposal |

## Phase results (browser-tested)
- **P1 Lead/CRM — PASS.** Created via UI; all fields persisted (DB-verified); pipeline counters updated correctly (+1 lead, +$950k).
- **P2 Qualification — PASS.** Qualifying scorecard 8/8 (live-saves each click). Stage gates enforced: Enquiry→Qualify (no gate), Qualify→Discovery (score≥5), Discovery→Winning Offer (3-item checklist: discovery notes + design stage + desired start), Winning Offer→Fee Proposal (pre-construction fee). All gate fields set via UI; advance buttons correctly enable/disable. Outcome stamps did NOT misfire on forward moves (won_at/lost_at stay null — verified).
- **P3 Fee Proposal / PTSA — PASS (live-fire of DOCX/mark-signed gap-documented).** PTSA panel: fee $15k carried from pre-con; service checkboxes; **"Scope not set — PTSA will show placeholder text" warning works**; "Generate PTSA Document" (DOCX) present; "Mark PTSA as signed" correctly **disabled until a signed PDF is uploaded** + helper text states it "creates the job folder" (Dropbox). Live DOCX-gen + mark-signed NOT fired (download/Dropbox side-effects) → gap-documented.
- **P4 Convert gate — PASS (W01-CONVERT-01 verified LIVE + in code).** `POST /convert-to-job` on the address-less lead → **400 "A site address is required before creating a project"**, `job_id` stayed null (no job, no Dropbox). Code: `convertLeadToJob` salesRoutes.mjs:263; PTSA mark-signed sets `siteAddressWarning=true` (salesRoutes.mjs:797) = PTSA-WARNING-01. Site address then set via UI → gate would unblock. **Actual convert (job + Dropbox folder) gap-documented — not fired.**

## Findings log
- **OBS-1 (info, not a bug):** Home dashboard "Could not load live data" was a **harness artifact** — my first injected token was rejected by the server (corrupted in transit). Base64 re-inject → 200, dashboard healthy. Server auth verified working (fresh token → 200). Not a Hub defect.
- **OBS-2 (Phase 1, by-design):** New-Lead quick-add form maps the location field to `leads.suburb`, leaving `leads.site_address` = null. Good — exercises the convert/PTSA address gate at Phase 4. Also `leads.name` (compound-client field) left null; first/last populated (salutation derives from those).

## Screenshots
| ID | Stage |
|----|-------|
| ss_7580j0uki | Login page (preflight) |

## Test users used
- (pending) Sam's admin session in Browser 1.

## Cleanup steps + result
- ✅ **DONE (2026-06-27).** Resolved leads by `email ILIKE '%BLH-E2E-20260627-1041%'` → 1 match. Deleted children (`lead_conversations`/`lead_notes`/`lead_documents` = 0 each) then the lead (`5367c278-…`). Re-query after = **0** (verified).
- No job/project created (convert gap-documented) → nothing else to remove. No Dropbox folders, emails, POs, or files fired this run.
- Scope strictly run-ID. No global cleanup, no `--confirm`, no touching of pre-existing legacy rows (`BatchA…`, `__DRYRUN…`, `BLH TEST W10/W18…`, `__DEMO…`).
- **Left intentionally for review:** this manifest + `docs/qa/E2E_FULL_WALKTHROUGH_BLH-E2E-20260627-1041.md`.
