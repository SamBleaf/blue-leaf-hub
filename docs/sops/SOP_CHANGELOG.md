# SOP Changelog — Blue Leaf Hub

Track every SOP change here. One row per change.

---

| 2026-07-22 | Finance — email scanner skips tender QUOTES + statements (only ingests invoices); clearer file-load error (SOP 09-02) | finance_review_ai_extraction.md (§12), SOP_CHANGELOG, financeRoutes.mjs, scripts/tests/finance-classify.test.mjs (NEW), scripts/reclassify-finance-quotes.mjs (NEW) | Sam: the admin inbox receives quotes AND invoices, and the scanner was calling tender quotes "invoices" to match (live check: 10 of 12 pending "invoices" were RFQ quotes/1 statement for the 2 Forrest Ave tender; only 2 real invoices). The amount-only gate let them through (quotes carry a price). New `classifyInboxDoc(extracted, {filename, subject})` gate in the email poller — AI `document_kind` (added to the extract prompt) + a deterministic net on filename+subject: keeps invoices/receipts, SKIPS quotes ("quote/quotation/RFQ/estimate/tender" with no invoice word) + statements; conservative (an "invoice"/"tax invoice"/"receipt" word always keeps it, so a real payable is never dropped; manual upload still forces one in). 19-assertion unit test built from the real inbox docs. Also: the "View PDF" storage error was the Dropbox app **missing the `files.content.read` scope** (uploads work, downloads 401 missing_scope) — the file-stream endpoint now returns an actionable message ("add files.content.read + re-run Dropbox auth") instead of the generic one; the actual fix is re-authorising Dropbox (Sam). Cleanup tool `reclassify-finance-quotes.mjs` (dry-run default) re-classifies the already-ingested pending docs → status='rejected' (reversible); dry-run shows the 10 candidates, apply pending Sam's OK. Lint + build + finance-classify 19/19 green. | Done | Claude (Opus) |
| 2026-07-22 | Worker PWA — assigned workers see their co-assignees (not just leading hands) (SOP 10-06 F3/TC-06) | worker_pwa_updates.md (§3 F3, TC-06), SOP_CHANGELOG, WorkerTasks.jsx | Sam: a worker on a task should see who they're assigned WITH, not only leading hands. The server already sends `assignees[{id,name}]` to every visible task (`attachAssigneesFromDb` on `GET /api/worker/tasks`) — this is a render-only change. A worker who's an assignee now sees, on the row, the avatar stack + "with [co-assignee first names]" (solo → "Assigned to you"), and the task detail sheet lists the full roster ("Assigned: you, Anna, Ben") for everyone. Not-assigned shared tasks stay unchanged (no roster shown). No visibility/permission change — a worker still only sees shared or own tasks. Lint + build clean. test_status: untested (device walkthrough pending Sam). | Done | Claude (Opus) |
| 2026-07-22 | Worker PWA — editable dictated tasks + hold-to-edit; Hub Plans mig-notice removed (SOP 10-06 F5, migs 152/153 live) | worker_pwa_updates.md (§3 F5, §5, TC-08), SOP_CHANGELOG, workforceRoutes.mjs, WorkerTasks.jsx, JobPlansCard.jsx | Sam applied migs 152+153 (live-verified: backfill correct, join path active) → **removed the "apply migration 152" notice** from the Hub Plans upload card (`JobPlansCard`, always shows the uploader now). **Editable tasks:** in the dictation/transcript draft list each task is now **tappable** to edit **name / info / category** before adding (was checkbox-only; `bulkAddDrafts` now also sends `description`); after a task is added a leading hand can **press-and-hold** it to reopen the same editor. Shared `TaskEditSheet` (name/info/category) + a `useLongPress` hook (500 ms, swallows the click that follows a fired hold so tap-to-complete still works; drag-reorder stays on the ⠿ grip, no conflict); wired on active/blocked/done rows, leading-hand-only (`canEditTasks = is_leading_hand && !preview`). Server: `POST /api/worker/tasks` accepts `description`; `PATCH /api/worker/tasks/:id` accepts `title`/`description`/`category` (leading-hand-gated, category validated against `SITE_TASK_CATEGORIES`). Lint + build + 12/12 + 44/44 green. test_status: untested (device walkthrough pending Sam). | Done | Claude (Opus) |
| 2026-07-21 | Worker PWA — 4-feature update: plans on device, supervisor crew view, multi-assign, timesheet autofill (migs 152/153) | worker_pwa_updates.md (NEW SOP 10-06), SOP_INDEX, SOP_CHANGELOG, MASTER_DATA_DICTIONARY §11, workforceRoutes.mjs, carpentryRoutes.mjs, taskAssignments.mjs (NEW), 5 PWA/Hub components, migs 152/153 | Single build (Step-0 inspected + plan-approved). **F1 Plans:** upload plans into the Hub → Supabase Storage (`job-plans` bucket) → PWA sees the CURRENT set; `job_documents` + `carpentry_job_id` (**mig 152**); supersession is EXPLICIT (uploader picks the replaced plan, never inferred); worker list + 60s signed-URL download gated by `workerVisibleJobs`; opened from Today + Tasks (bottom sheet, no new route). **F2 Crew view:** Week page gains a leading-hand-only My-week/Crew toggle; `GET /api/worker/crew/day` (same-site/same-day roster + trade, avatar stack + expand); refetch-on-focus (no realtime — table is RLS deny-all). **F3 Multi-assign:** `site_tasks.assigned_to` → `task_assignments` join (**mig 153** + backfill); probe-gated **dual-read/write** keeps `assigned_to` mirrored to the first assignee (both embed aliases + rollback safe pre/post-mig); the `assigned_to IS NULL OR = me` predicate across 6 server spots → `visibleToWorker` (shared OR I'm on it); multi-select picker (rostered-that-day scope + show-all) + `AssigneeStack` across worker/carpentry/charge-up/ops/team; new `.../tasks/:id/assignees` endpoints (worker leading-hand + office admin/supervisor). **F4 Autofill:** `WorkerLogHours` pre-fills the site from the day's allocation (editable default, keyed to the entry's date). PWA "supervisor" = `employees.is_leading_hand`. Mandatory cutover tests A (mirror re-points on unassign) + B (no leak of others' tasks) pass PRESENT and ABSENT (12 assertions); charge-up 44 still green; lint + build clean; adversarial review of the cutover + gating. test_status: untested (migs 152/153 apply + device walkthrough pending Sam). | Done | Claude (Opus) |
| 2026-07-21 | Charge Up — site detail pop-up now tabbed (Shifts · Tasks · Diary) reusing the carpentry job tables/components (mig 151, SOP 10-05 v1.4) | charge_up_sites.md (§3/5/10, TC-12/13), SOP_CHANGELOG, MASTER_DATA_DICTIONARY §11, chargeUpRoutes.mjs, CarpentryJobDetail.jsx, CarpentrySiteDiary.jsx (NEW), ChargeUpTasksPanel.jsx (NEW), ChargeUpSiteDetailModal.jsx, media.js (NEW), mig 151 | Sam: "lean heavily on the carpentry modules coding for jobs for all of these features." Chose the middle path — a charge-up **site** (not a carpentry_jobs row) gains per-site **Tasks + Diary** by re-keying the SAME tables to `charge_up_job_id` (**mig 151** adds it to `site_tasks` + `carpentry_site_diary`, widens the site_tasks one-owner XOR to 3-way, drops the diary `job_id` NOT NULL). Charge-up tasks/diary are **owned by the site alone** (parent ids NULL) so they never leak into a parent-job reader (worker PWA task list, job Tasks/Diary tabs, earned-value rollup) — no hot-path reader edits. Backend reuses those tables + `signSiteTaskPhotos` + the id-scoped `PATCH/DELETE /tasks/:id`; new `GET/POST /charge-up-jobs/:id/tasks` + `…/diary` (fail-soft pre-mig via `isMissingTable`). Frontend: **extracted** the job's DiaryTab diary half into a shared `CarpentrySiteDiary` (the **job page renders the same component** — verified no regression), a lean `ChargeUpTasksPanel` (same endpoints, no job-only budget/template/transcript), a **tabbed** `ChargeUpSiteDetailModal` (Shifts · Tasks · Diary) reusing `KpiCard` / `MobileTabs` / a shared `mediaUrl`. Editing gated on `can.accessCarpentry` (admin+supervisor); cost/margin still director-only. Lint + build + 44 unit tests green; adversarial regression review. test_status: untested (mig 151 apply + live walkthrough pending Sam). | Done | Claude (Opus) |
| 2026-07-21 | Charge Up — per-site pricing switched from flat rate to TARGET GROSS MARGIN (mig 150, SOP 10-05 v1.2) | charge_up_sites.md (§3/4/5/10, TC-11), SOP_CHANGELOG, chargeUpService.mjs, chargeUpRoutes.mjs, ChargeUpJobDetail.jsx, scripts/tests/charge-up.test.mjs, MASTER_DATA_DICTIONARY §11, mig 150 | Sam, on testing mig 149: "I don't want a flat hourly rate next to the jobs — I want a **margin on wages** that I can adjust." Chose **target gross margin** (over markup). Replaced the per-site `charge_out_hourly` with `charge_up_jobs.margin_pct` (**mig 150** drops the mig-149 column — no prod data — and adds `margin_pct numeric(5,2) CHECK 0≤x<100`). New pure `chargeOutFromMargin(cost, m) = cost ÷ (1 − m/100)`; `rollupBySubJob`/`rollupByFinancialYear` take `marginBySite` and, when a site has a margin, price charge-out off the **wage cost** so the realised gross margin equals the number set (else per-worker `charge_up_hourly`). **Director-only** (margin reveals cost): PATCH `marginPct` rejects non-admin + the UI field/badge gate on `showCost`; `marginPct` stripped from the summary for non-directors. Inline `Margin %` field per site (replaces `$/hr`), live recompute; validated 0–99.99. Fail-soft pre-mig-150 (`select("*")` + `marginReady` column-detect hide the field). Live-simulated on real data: Bickford $2,403 cost → **40% → $4,005 charge-out (exactly 40%)**, Moana → 30% → $1,668. 42 unit tests green + lint + build. test_status: untested (mig 150 apply + live walkthrough pending Sam). | Done | Claude (Opus) |
| 2026-07-20 | Charge Up rework — per-shift drill-down + gross margin + per-site rate override (mig 149, SOP 10-05 v1.1) | charge_up_sites.md (§3/4/5 +flow, §10, TC-09/10/11), SOP_CHANGELOG, chargeUpService.mjs, chargeUpRoutes.mjs, ChargeUpJobDetail.jsx, scripts/tests/charge-up.test.mjs, mig 149 | Sam: the Charge Up screen showed "very limited information", couldn't "adjust margin per job", and the PWA/timesheet detail wasn't reachable from the job header. Independent scope review (workflow: 4 readers + 3-lens judge panel) trimmed ChatGPT's 9-section billing spec to **Phase 1 (read-layer, no migration) + Phase 2 (one column)** — cutting the persisted draft-invoice generator, per-entry overrides, and task_category grouping (dead: PWA charge-up is always 'other'). **Phase 1:** `/charge-up-summary` now returns per-site `entries[]` (date · worker · **free-text note** · hours · charge-out · cost) + `lastDate`; the site-row expander lists every **shift** (was per-person aggregate only); a **gross-margin** KPI tile + per-site Margin column (director-gated, derived from cost); untagged rows show the worker's note. **Phase 2 (mig 149):** `charge_up_jobs.charge_out_hourly` — a flat per-site charge-out rate that **overrides** each worker's charge_up_hourly (rollup fallback; blank = per-person); inline `$ rate /hr` editor per site with live recompute; PATCH whitelists `chargeOutHourly` (0–999999.9999). All rate/cost paths stay derived + director-gated (`stripCost` also nulls entry cost); fail-soft pre-mig-149 (`select("*")` + `rateReady` column-detect hide the editor). Adversarial review (3 lenses + verify): **2 confirmed + fixed** — FY rollup now takes `rateBySite` so by-FY charge-out reconciles with per-site totals; rate PATCH upper-bounded to avoid numeric(10,4) overflow 500. Live-verified vs prod (108h/$7,845/$6,656 match; notes surface; margins 16%/10%). 39 unit tests green + lint + build. test_status: untested (mig 149 apply + live walkthrough pending Sam). | Done | Claude (Opus) |
| 2026-07-19 | Carpentry schedule — editable per-category crew size + auto-recompute end (mig 148, SOP 10-04/14-03) | workforce_pipeline.md (§3), cost_intel_carpentry_budget_margin.md, MASTER_DATA_DICTIONARY §11, carpentryStageScheduleService.mjs, carpentryStageScheduleRoutes.mjs, CarpentryJobDetail.jsx (ScheduleTab) | Sam: the whole-team duration assumption was inaccurate — each category runs a different crew. **Re-enabled crew-scaling** in `costModelStageDays` (= labour_sell ÷ team-day-rate × headcount/crew) and added an **editable 👷 Workers cell per category** on the Schedule tab (**mig 148** `carpentry_job_stage_schedule.crew_size`, default from CREW_DEFAULTS, persists through re-auto-layout like a lock). **Changing a category's start date — or its Workers count — now auto-recomputes the end** from the value-based duration (PATCH recompute; an explicit end/drag still wins). GET returns `crewSize` + `valueDays`. Fail-soft pre-mig-148 (crew_size stripped on persist, defaults shown). This SUPERSEDES the earlier "reconcile to whole-team Days @ margin" call (2026-07-19, same day) — the budget's Days @ margin stays whole-team; the schedule scales to the real crew (equal only at full team). Stage-schedule tests reverted to crew-scaled + crew-persistence added (38 green). Dictionary §11 +`crew_size`. Deployed. test_status: untested (mig 148 apply + live walkthrough pending Sam). | Done | Claude (Opus) |
| 2026-07-19 | Budget-spine alignment Phase 2 — earned value at sub-task grain (SOP 14-03) | cost_intel_carpentry_budget_margin.md (§5), carpentryRoutes.mjs (budget), CarpentryJobDetail.jsx (SubtaskSections), docs/plans/BUDGET_SPINE_ALIGNMENT.md | Builds on Phase 1's per-sub-task actuals. The budget drill-down is now a real per-sub-task **earned-value** view: each sub-task shows sell vs **actual** (mig-147 timesheets) vs **variance** (red when over) + hours; a footer surfaces the category's **untagged actual** (`line.untaggedActual` = category actual − Σ sub-task actuals) so legacy/coarse hours are visible and reconcile. No new migration; category-level margin already includes all logged hours (no double-count). Lint + build clean. The schedule↔budget join-hardening (off fragile slug(category_name)) is deferred to a hardening pass (touches the auto-heal drift logic). test_status: untested (populates once mig 147 applied + sub-task hours logged). | Done | Claude (Opus) |
| 2026-07-19 | Budget-spine alignment Phase 1 — sub-task is a first-class timesheet identity (mig 147, SOP 14-04) | cost_intel_log_time_subtasks.md, MASTER_DATA_DICTIONARY §11, subtaskRollup.mjs (NEW), workforceRoutes.mjs, carpentryRoutes.mjs, WorkerLogHours.jsx, CarpentryJobDetail.jsx, docs/plans/BUDGET_SPINE_ALIGNMENT.md (NEW) | Sam: budget categories/subcategories are the source of truth for schedule, values, allowed days, and timesheet subcategories — a deep audit (live J1120) found NO single sub-task identity (4 labels / 3 grains; 0 of 94 approved entries tagged a sub-task → per-sub-task actuals dead). Sam chose **Full + PWA sub-task required**. Phase 1: **mig 147** adds `timesheet_entries.canonical_key`; a carpentry hour attributes to `(task_category, canonical_key)`. PWA (`WorkerLogHours`) keys entries by canonical_key (was budget_line_item_id) + a has-subtasks category can only be logged via the sub-task chooser (required, UI-enforced); `POST /worker/timesheets` validates+stores canonical_key **probe-gated** (fails soft/coarse pre-mig); Approvals label + `/timesheets/:date` echo it. `GET /budget` rolls per-sub-task actual by (workforce_task_category, canonical_key) via pure `subtaskRollup.mjs` (8 tests) → shown on each budget sub-task group. Labels unified on the dictionary (Phase 0, `3d25d40`). 90 test assertions green + lint + build + live-gate verified; multi-agent review stalled (infra timeout) so this rests on tests + careful probe-gating. Dictionary §11 +`timesheet_entries.canonical_key`. test_status: untested (mig 147 apply + live PWA→approve→budget walkthrough pending Sam). Phases 2-3 (margin consumes sub-task grain; material coverage + persist durations + fold site_tasks) follow. | Done | Claude (Opus) |
| 2026-07-19 | Carpentry projected-margin model + stage-duration reconcile + pipeline job link (SOP 14-03, 10-04) | cost_intel_carpentry_budget_margin.md (§5), workforce_pipeline.md (§3), marginProjection.mjs (NEW), carpentryRoutes.mjs (budget), carpentryStageScheduleService.mjs, CarpentryJobDetail.jsx, StageEditor.jsx | Sam feedback on the carpentry budget money engine. **① % done** now driven by the STAGE SCHEDULE (complete→100 / planned→0 / in-progress→blend of schedule-elapsed + logged-cost-vs-allowable), not the ticked site_tasks ratio (which is now only a fallback for unscheduled jobs). **② Projected margin** rewritten target-anchored: projectedCost = actual + allowable×(1−pct), allowable=budget×(1−target) — baselines at 25/20 and deviates only on real burn; an **evidence clamp** holds it at target (with a `*` flag) instead of the old actual÷%done which read a phantom 97–100% for barely-logged/complete stages. The snapshot badge now flips to "Tracking below target" from the projection. New pure `marginProjection.mjs` (23 tests). **④ Stage duration** reconciled to the budget's whole-team "Days @ margin" — `costModelStageDays` dropped the crew-scaling factor (Cladding ~8d not ~14d), fixing the sequential-layout double-count. **③ Pipeline** stage-editor job name is now a link to `/carpentry/:id?tab=schedule` (deep-link tab support added). Live-verified on J1165 (all phantom 100%→25%+flag). Adversarial review: 2 confirmed + fixed (job-level fallback floors at actual spend so overspend isn't erased; scheduleElapsed ignores reversed windows). Stage-schedule tests updated for whole-team durations (33). test_status: untested (live UI walkthrough pending Sam). | Done | Claude (Opus) |
| 2026-07-19 | Charge Up header/untagged + Pipeline year-start/complete + Scheduler lock/subsections (SOP 10-04, 10-05) | charge_up_sites.md (§5 +2 flows, TC-08b/08c), workforce_pipeline.md (§3, TC-15/16/17), chargeUpService/chargeUpRoutes, carpentryStageScheduleService/Routes, PipelineCalendar.jsx, CarpentryJobDetail.jsx, ChargeUpJobDetail.jsx | Five follow-ups (no new migration). **Charge Up ①** category summary header (KPI strip: hours / charge-out $ / active sites, director-gated cost) + a **by-financial-year** table with charge-out $ (new pure `auFinancialYear`/`rollupByFinancialYear`; `byFy` added to charge-up-summary; the older internal-cost-summary lacked charge-out). **② Assign untagged hours** to a site — new `GET /charge-up-untagged` + `POST /charge-up-assign` (belongs-to guarded: only this job's approved-timesheet entries), amber card lists untagged entries with per-row + assign-all site pickers → hours move into per-site figures. **Pipeline A1**: completed stages (`scheduleStatus==='complete'`) drop off the calendar (toBlocks filter). **A2**: year view now starts the **previous** month and rolls **13 months** (seedCursor month−1 + YearView rebuild; onZoom carries real {y,m}; months labelled with year). **Scheduler B1**: the indistinguishable 🔒/🔓 emoji → a labelled **🔒 Locked / 🔓 Lock** pill (wiring was already correct; pure affordance fix). **B2**: each stage row on the Schedule tab expands to its budget **subsections** (line items grouped by canonical_key) with cost-model durations (new pure `subsectionsForStages`; display-only/derived per Canonical Data Law §3; stage GET carries `subsections`; patchStage preserves them). Unit tests +13 (charge-up 26, stage-schedule 32; all green). Live-data verified (Naldera subsections; 5 untagged entries; FY rollup). Adversarial multi-agent review (3 lenses + verify): 1 low finding fixed (isMissingTable now matches missing-column 42703 so untagged/assign fail soft pre-mig). test_status: untested (live UI walkthrough pending Sam). | Done | Claude (Opus) |
| 2026-07-19 | BLB Charge Up — Planner shift → site (SOP 10-05 update, mig 146) | charge_up_sites.md (§3/4/5/10/14 +TC-08), SOP_CHANGELOG, MASTER_DATA_DICTIONARY §11 | Per Sam: a **charge-up shift on the Planner must name its site (address)**. Dropping BLB Charge Up on a shift cell now opens a **"Which charge-up site?"** picker; the shift can't be saved without one, and the cell then shows the **site** (not just "BLB Charge Up"). Fill-across-days / fill-down / move / duplicate all carry the site. New column `workforce_allocations.charge_up_job_id` (**mig 146**, ON DELETE SET NULL); `POST /api/workforce/allocations/assign` (+ POST/move) accept `chargeUpJobId`, validated by the pure `validateChargeUpSite` (required + belongs-to) in `chargeUpService.mjs` (6 new unit tests). `GET /api/workforce/allocations` echoes `chargeUpSiteLabel`/`chargeUpSiteAddress`. Fails soft before mig 146 (labels null; shift saves untagged — Planner never breaks). SOP §3/§4/§5/§10 updated + TC-08. Dictionary §11 +`workforce_allocations.charge_up_job_id`. test_status: untested (mig 146 apply + live Planner drag walkthrough pending Sam). | Done | Claude (Opus) |
| 2026-07-18 | BLB Charge Up — site-level charge-up tracking (SOP 10-05, NEW) | charge_up_sites.md (NEW), SOP_INDEX, MASTER_DATA_DICTIONARY §11 | NEW SOP 10-05 for the BLB Charge Up feature. BL-CHARGEUP (a permanent carpentry job, project_type='other') becomes a **category of small "sites"** (`charge_up_jobs`, mig 145) with its own detail layout (branched on reference — BL-INTERNAL keeps the standard tabs). Workers pick BLB Charge Up on the app → a **required Location** → hours tag to the site (`timesheet_entries.charge_up_job_id`, clone of mig 141). **Cost rolls to the category; hours + charge-out $ (approved hours × each person's charge-up rate from the cost model) track per site + per person** for invoicing; internal cost director-gated. Endpoints: charge-up-jobs CRUD + charge-up-summary (`chargeUpService.mjs` rollup, no calc in routes) + worker subtasks/submit extended with `charge_up_job_id` (belongs-to guard; required only when the category has active sites — backward-compatible pre-mig-145). Full 14-section template; TC-01..TC-07; test_status: untested (mig 145 apply + live PWA-log/approval walkthrough pending Sam). Dictionary §11 +`timesheet_entries.charge_up_job_id`. Commits (P1) b695c38 · (P2) aef457e · (P3 this). | Done | Claude (Opus) |
| 2026-07-18 | Workforce Pipeline v2 — interactive stage Calendar (SOP 10-04 → v2.0) | workforce_pipeline.md, dictionary §11 | REDESIGN per Sam: the Pipeline gains an interactive **stage Calendar** (Year default / Month) alongside the forecast Timeline. Each carpentry job breaks into its **budget labour subsections** as calendar blocks; **stage length = labour value ÷ team day-rate** (the earned-value engine — more labour $ → longer stage). **Month view drag** reschedules a stage and **ripples** its dependents forward (reuses `scheduleUtils.previewRipple`); **click** opens an editor (dates, ±shift, lock). All writes hit the new `carpentry_job_stage_schedule` (migration 144) — the SAME store the carpentry job **Schedule tab** uses, so edits **sync both ways** (the milestone list is retired). Approved timesheets (by workforce task_category) show **actual vs planned** on each block. SOP §3 rewritten (Calendar + Timeline), §14 +TC-10..TC-14 (stage blocks, drag+ripple, two-way sync, lock/●-started, Year default). Backend: `carpentryStageScheduleService` (budget-driven seed), `carpentryStageScheduleRoutes` (auto-heal + clean reseed), `stageRipple.js`, `PipelineCalendar`/`StageEditor`. Commits 52e6834/8a3146e/06f0e32/f5ec919 + Phase 3 actuals. Migration 144 (+ idempotent ADD COLUMN). test_status: untested (live drag verification pending Sam). | Done | Claude (Opus) |
| 2026-07-18 | Workforce Pipeline — Capacity & Schedule Intelligence (SOP 10-04, NEW) | workforce_pipeline.md (NEW), SOP_INDEX, MASTER_DATA_DICTIONARY §11 | NEW SOP 10-04 for the Workforce **Pipeline** tab — a forward-looking, read-only capacity + scheduling decision tool (admin/supervisor, same gate as Planner). Documents the **four schedule measures kept separate** (Committed real schedule · Expected deterministic forecast · Break-even allowance financial limit · Actual from approved timesheets), the **margin-risk flag** (expected productive crew-days > break-even allowance), the **crew-capacity band** (available vs committed real allocations + forecast demand; construction shown as context, internal-labour only — never full span), and confidence tiers (live rate > comparable history > budget-derived). Grounded in `GET /api/workforce/pipeline` + `/forecast/:id` and the deterministic services (workingCalendar / carpentryStages / stageAggregation / scheduleIntelligence / workforceCapacity — no AI, no business math in route/UI). Break-even reconciles to the Budget burn block (`costModelService.burnForLine`) — unit-proven; TC-06 checks it against a real job. Full 14-section template; TC-01..TC-09; test_status: untested (live authenticated walkthrough + 2-real-job break-even reconciliation pending Sam). No migration (stages map in code). Dictionary §11 +4 Generated schedule facts (expected_duration_crew_days, expected_completion_date, break_even_allowance_days, schedule_margin_risk). Deferred/optional capability parked in docs/plans/WORKFORCE_PIPELINE_FUTURE_TODO.md. Unit tests: scripts/tests/workforce-pipeline.test.mjs (73 assertions, green). | Done | Claude (Opus) |
| 2026-07-14 | Earned-Value Costing SOPs (14-03, 14-04, 14-05) | cost_intel_carpentry_budget_margin.md (NEW), cost_intel_log_time_subtasks.md (NEW), cost_intel_pricing_intelligence.md (NEW), SOP_INDEX | EARNED-VALUE COSTING SOP SET — 3 new SOPs for the carpentry earned-value system (Phases 1–4 + material sub-task tagging). **14-03** Carpentry budget, sub-task mapping & margin gauge: estimate import/re-import seeds budget + sub-task line items; margin thermometers (labour 25% / material 20%) with projected margin from task completion; the human-confirmed sub-task mapping drawer (move/add/delete → Save & confirm); grounded in `GET/POST /api/carpentry/jobs/:id/budget[/seed|/line-items|/confirm]` + `carpentry_budget_line_items` (mig 140). **14-04** Log time against sub-tasks (Worker app): two-level PWA picker (main category → sub-task chooser), entries carry `budget_line_item_id`; `GET /api/worker/jobs/:id/subtasks` + `POST /api/worker/timesheets` (mig 141). **14-05** Pricing intelligence & Approvals task view: inline Task column on Approvals (sub-task label), the Carpentry Pricing-intelligence board with completed/all + category/subtask toggles + realised loaded margin; `GET /api/carpentry/pricing/streams?scope=&grain=` + material cost tagging (mig 142). Full 14-section template; TC-01..05+ each; test_status: untested (migrations 140/141/142 manual-apply + live E2E pending). SOP_INDEX +14-03/04/05. | Done | Claude (Opus) |
| 2026-07-02 | SOP-DOCS-WAVE-04 — remaining folders + marketing (docs-only) | 00_getting_started (3), 01_global_navigation (2), 03_tendering (2), 05_operations (2), 06_scheduling (8), 09_finance (12), 18_marketing_agent (7), 19_marketing_intelligence (2) | HARDENING LOOP (Cursor role, 8 Sonnet agents via workflow; Claude-reviewed). §14 compliance + accuracy across 38 SOPs; all now end at §14. **Accuracy fixes vs real app:** 00 login has no Forgot-Password (Remember-me toggle), /signup is invitation-only dead-end, accept-invite needs Full name; 01 sidebar/nav items match `AppShell.jsx` + `ProjectBar.jsx` (sticky strip, mobile bottom sheet); 03 tender board 4 view-modes + archive via `POST /api/tender/archive` (+ unarchive, 409 RFQ-delete guard); 05 ops global-gantt + buildexact-link; 06 scheduling all 8 to canonical template; 09 finance all 12 to canonical + corrected 3 stale API paths; 18 marketing Command-Centre SOPs §13/§14 fixed; 19 intelligence + 19-09 Attribution-ROI restructured 15→14 sections. **Marketing UNPAUSED per Sam.** Docs-only; guard verified (only docs/). **SOP §14 sweep now COMPLETE for all folders 00–17 + marketing 18/19.** Note: 6 superseded pre-rebuild marketing SOPs (18-02_generate_content_ai.md etc.) remain on disk as cleanup candidates — not added to the index. | Done | Claude (loop controller) + Sonnet agents |
| 2026-07-02 | SOP-DOCS-WAVE-03-ii — admin-settings + subcontractors + cost-intelligence §14 (docs-only) | 12_admin_settings (6), 13_subcontractors (3), 14_cost_intelligence (2) | HARDENING LOOP (Cursor role, Sonnet agents; Claude-reviewed). §14-compliance + accuracy across 11 SOPs (all were §12-only → now 14-section, test script §14 last). **12 (admin):** company-details rewritten to localStorage-only (no server branding API), Buildexact corrected to the real UI flow (POST /api/buildexact/test-connection), "Settings → Integrations → X" → "scroll to the X section" (no sub-nav; Xero UI is planned/not-built). **13 (subcontractors):** corrected lookup signature (business_name/email/trade/suburb/state, not query/abn), required fields (business_name+email+trade), client-side CSV import. **14 (cost-intel):** pre-tender + benchmarks corrected to real UI/API/DB behaviour. Docs-only; guard verified. **Product items logged (deferred):** pretender_estimates insert has no idempotency guard (dup rows); benchmarks-tab naming confusion; consent-fields ignored (from 03-i). **Wave 03 COMPLETE (folders 12–17; marketing 18/19 paused).** | Done | Claude (loop controller) + Sonnet agents |
| 2026-07-02 | SOP-DOCS-WAVE-03-i — CRM/mailing + procurement + carpentry §14 (docs-only) | 17_crm_mailing_list (4), 16_procurement (10), 15_carpentry (6) | HARDENING LOOP (Cursor role, Sonnet agents; Claude-reviewed). §14-compliance + accuracy sweep, all 20 SOPs normalised to the 14-section template (test script §14, last). **17 (CRM):** corrected consent-at-create step, smart-list manual-only add, relationship-score breakdown (call/meeting/site_visit only), documented Batch 1B lead_id backfill + ContactDrawer "Full history". **16 (procurement):** replaced non-canonical §10–§13 headings across all 10; v1.1. **15 (carpentry):** no-auto-seed on job-create, transcript task drafts editable (category/priority) + work-stream-aware AI, Worker PWA hold-drag reorder documented, closeout→carpentry_job_performance; v1.1. Docs-only; guard verified (only docs/). **Product bugs logged (deferred, Sam-gated):** New-Contact form collects consentToEmail/consentSource that `POST /api/crm/contacts` silently ignores (no consent column); 16-06 stale "Phase 2" note (draft-po is shipped); migration 092 (supplier perf) not in the CLAUDE.md migration table. | Done | Claude (loop controller) + Sonnet agents |
| 2026-07-02 | SOP-DOCS-WAVE-02B-ii — sales Pass 3A + RFQ nav + workforce §14 (docs-only) | 02_sales/02-01..02-07 (7), 04_rfq_engine/04-02..04-09 (8), 10_workforce/workforce_overview.md, SOP_INDEX | HARDENING LOOP (Cursor role, Sonnet agents; Claude-reviewed + section-normalised). **Sales (SOP-DRIFT-02-SALES):** rewrote 02-01..02-07 (v2.0) to the real Pass 3A Lead command-centre UX (LeadCommandCentreLayout two-column + sticky rail, LeadStageStepper, advisory gates, Log Activity in-workspace, Qualifying ScoreGate in focus panel, Blueprint auto-loading right rail, ConversationPanel input→analysing→review, lead_source now required); 02-08/02-09 untouched. **RFQ:** corrected 04-02..05 = Engine wizard (/tender-manager/rfq-engine) vs 04-06..09 = Quote Tracker (/tender-manager/rfq-packages/:id) with explicit routes. **Workforce:** 10-01 §14 fixed + documented leading-hand add-task/drag-reorder (TC-10); 10-02/10-03 already compliant. All test scripts normalised to §14 (14-section template). Docs-only; guard verified. Logged for a future product wave: dead `QuoteTracker.jsx` (redirect → RfqPackageDetail); no conversation read-view; Blueprint extended-chat pointer. | Done | Claude (loop controller) + Sonnet agents |
| 2026-07-02 | SOP-DOCS-WAVE-02B — Client Portal v2-canonical (docs-only) | 00_PORTAL_STACK_MATRIX.md (NEW), portal_*.md (9), 11-12_v2_admin_console.md, SOP_INDEX | HARDENING LOOP (Cursor role, Sonnet agent; Claude-reviewed). Closes HIGH deploy-blocker **SOP-DRIFT-SEC14-11** (SAM-SOP-001, v2 canonical). Created `00_PORTAL_STACK_MATRIX.md` (v1↔v2 routes/SOP cross-reference; v2 canonical for new jobs, v1 = legacy/fallback). Added a LEGACY banner to all 9 `portal_*.md` (point to v2 SOPs 11-10..13); added §12 Edge cases + §13 Owner + renumbered the test script to §14 (TC-01..06) across the legacy set; bumped sop_version + last_reviewed. Accuracy fix in v2 SOP 11-12: confidence enum `at_risk`→`watch` (matches migration 103 CHECK + frontend). Docs-only — no product code; guard verified (only docs/ touched). | Done | Claude (loop controller) + Sonnet agent |
| 2026-07-02 | SOP-DOCS-WAVE-02B — Site diary §14 + view-only fix (docs-only) | diary_view_entries.md, diary_write_entry.md, diary_voice_capture.md, SOP_INDEX | HARDENING LOOP (Cursor role, Sonnet agent; Claude-reviewed). Closes **SOP-DRIFT-SEC14-07**. Renumbered the troubleshoot test script to §14 (was §12) and inserted §11 Screenshots / §12 Edge cases / §13 Owner across all three (TC-01..07 each). **07-03 corrected to view-only** (verified against `SiteDiary.jsx` + `siteDiaryRoutes.mjs`): removed the fictitious Edit + date-range-filter steps, marked the PATCH/date params "not implemented", and linked the deferred **SOP-BUG-07-03** (app not changed). Renamed 07-03 in the index to "View past diary entries (view-only)". Docs-only; guard verified. | Done | Claude (loop controller) + Sonnet agent |
| 2026-07-02 | CRM / Sales Control System (Batches 1A–1C, migrations 127–130) | 02-08 (NEW), 02-09 (NEW), 19-09 (NEW), SOP_INDEX | CRM CONTROL SYSTEM SOP SET — 3 new SOPs for the CRM/Sales control build. 02-08 Classify lead fit & work the action queue (Batch 1A): fit_quality × readiness manual tags, 8-bucket driven action queue with rule-based due-date defaults + snooze, mandatory lead_source_category on every create path — grounded in `PATCH /api/sales/leads/:id`, `deriveActionForStage()`, `normalizeLeadSourceCategory()`; §14 mirrors `npm run test:w1a-crm-control-spine`. 02-09 Lead trust rail & unified timeline (Batch 1B): trust-context rail (first/last touch + objections/fears/priorities from lead_signals) + `v_lead_timeline` unified stream (activities+notes+conversations+CRM+email opens/clicks) + convert backfill of crm_interactions.lead_id + `scripts/backfill-crm-timeline.mjs`; grounded in `GET /api/sales/leads/:id/timeline` + lead_signals CRUD; §14 mirrors `npm run test:w1b-timeline-signals`. 19-09 Attribution ROI — source→fit→won (Batch 1C): `v_lead_attribution_roi` per-lead view + `GET /api/intelligence/attribution-roi` grouped table + dashboard pipeline_value/won_value KPI (was null) + won-value writeback + fee_proposals.lead_id trigger; §14 mirrors `npm run test:w1c-attribution-roi`. All endpoints/UI degrade softly if migrations 127–130 unapplied. Full 14-section template; TC-01..TC-06 each; test_status: untested (migrations manual-apply + server run pending). SOP_INDEX total 122→125. | Done | Claude (Opus, adversarial workflow audit) |
| 2026-06-29 | SOP Docs Wave 02A — WHS Setup SOP | 08-07_whs_setup.md (NEW), SOP_INDEX, SOP_DOCS_WAVE_02_RESULT.md | NEW SOP 08-07 (SAM-SOP-002) — WHS Setup / risk engine (`/operations/:projectId/whs-setup`, WhsEngine). Full 14-section template + Section 14 (TC-01..TC-06) grounded in real endpoints: `GET/PUT /api/whs/projects/:id/profile`, `POST …/generate/project_whs_management_plan`, `GET …/documents`; tables `whs_site_profiles`, `whs_documents`. **Wave 02 split for size (sanctioned):** 02A = 08-07 only. Sharper diagnosis recorded: the 07_site_diary §14 finding is **wrong section number (test script at §12) + 07-03 content drift (Edit/date-filter not in app, ties to deferred SOP-BUG-07-03)**, not a literal missing test script — re-scoped into Wave 02B. | Done | Cursor (Wave 02A) |
| 2026-06-29 | SOP Module Audit Wave 01 (lead→handover) | 04-01, 05-05, 05-06, 08-02, 10-01; BUG_REGISTER SOP section; SOP_MODULE_AUDIT_WAVE_01_RESULT.md | SOP-DRIFT fixes — RFQ Engine vs Quote Tracker split (04-01); Operations global Gantt + trade conflicts as dashboard panels not separate pages (05-05, 05-06); WHS Contractors tab label (08-02); Workforce magic-link auth + Snapshot/Planner/Team tabs (10-01). Full wave audit logged 71 SOPs; 25 missing §14, 20 partial TC; substantive app bugs logged for Fix Agent (02-07, 05-05 click, 07-03, 09 Job View, 11-12). Portal legacy vs v2 = ACCEPTED-GAP candidate for Sam. | Done | Cursor (audit wave) |
| 2026-06-28 | Marketing Hardening Prep Batch 1 — API auth hardening | 18-01..18-08 (no step changes — security posture note) | SECURITY (defense-in-depth, no behaviour change for admins) — added explicit `requireRole("admin")` to every endpoint in the 5 mutation-bearing marketing route modules (marketingRoutes, marketingPackageRoutes, marketingScheduleRoutes, marketingLibraryRoutes, marketingCampaignRoutes). Previously these relied solely on the blanket `/api/marketing` prefix gate for the admin check; now each route self-enforces, so an accidental change to the prefix list at merge cannot silently drop sensitive writes to any-authenticated-staff. Non-admin behaviour unchanged (still 403). Also: finance invoice IMAP poller now honours `INVOICE_IMAP_POLL_ENABLED` (default ON) so it can be disabled for an isolated smoke boot; removed orphan ContentCreatorShell.jsx. No API contract, route, or schema change — SOP steps unaffected. | Done | Claude (Opus) |
| 2026-06-28 | Marketing Completion Batch 2 — Content Studio & package-review polish | 18-01, 18-04 (copy refresh only — no step changes) | UI POLISH (no behaviour change) — Content Studio (`/marketing/studio`) clarified: numbered steps (1 Source · 2 Decisions · 3 Review & send), audience/platform helper text, a generate disabled-reason hint, a "This package" metadata summary (angle / source photo / audience / platforms), per-draft "edit before posting" caption, and an expanded "what happens next" message after sending to the Approval Queue (review → approve → schedule → post manually). New shared `ReviewLegend` (label + risk-level meanings + the manual flow) reused in the Studio and the Approval Queue so the review vocabulary is identical in both. `MediaPickerModal` aligned to the shared demo banner + demo-only-on-unreachable. No API/route/schema change; existing SOP steps still hold — 18-01 (Studio overview) and 18-04 (package review) may want a screenshot/copy refresh at the next audit. test_status unchanged. | Done | Claude (Sonnet) |
| 2026-06-28 | Marketing Completion Batch 1 — Command Centre UI polish | 18-01, 18-07 (copy refresh only — no step changes) | UI POLISH (no behaviour change) — standardised the marketing data-state vocabulary across all surfaces via a shared `MarketingStateBanner` (Demo / Empty / Error): demo content now appears ONLY when the API is unreachable (no longer on a successful-but-empty response), so a live-empty screen reads as "nothing yet" not "demo". Removed stale "needs migration 122 / staging" banner copy (122 is applied). Added a collapsible **Module readiness** panel + a "weekly loop" workflow strip to the Command Centre, and field helper text (Media Vault filters, Weekly Planner template/slot/channel, Studio from-media/from-idea). No API, route, or step changes — existing SOP procedures still hold; 18-01 (Studio overview) and 18-07 (Intelligence/Attribution) may want a screenshot/copy refresh at the next SOP audit. test_status unchanged (runtime verification still deferred to staging). | Done | Claude (Sonnet) |
| 2026-06-28 | Marketing module rebuild (Run A → Batch 3) | 18-02 through 18-08 (new/replaced), SOP_INDEX | MARKETING SOP SUITE REBUILT — 7 SOPs replaced/created for the new Marketing Command Centre IA. 18-02: Weekly Marketing Planning (Weekly Planner + templates, replaces old AI generation SOP). 18-03: Media Capture and Upload (Media Vault browse + filters + asset seeding, replaces old photo generation SOP). 18-04: Content Package Review and Approval (Approval Queue approve/request/reject, replaces old review SOP). 18-05: Calendar Scheduling and Manual Publishing (Calendar week view + mark as posted, replaces old campaigns SOP). 18-06: Evergreen Library (evergreen_score + library browse, replaces old media upload SOP). 18-07: Marketing Intelligence and Attribution (Intelligence pipeline tiles + Attribution source breakdown, replaces old music library SOP). 18-08: Staging Runtime Smoke Checklist (NEW — consolidated pre-merge checklist for all marketing surfaces). All SOPs follow the 14-section template with TC-01 through TC-05+ in Section 14. Old pre-rebuild SOPs (18-02_generate_content_ai, 18-03_upload_photo_generate_content, 18-04_review_approve_content, 18-05_create_manage_campaigns, 18-06_upload_manage_media, 18-07_music_library) remain on disk as cleanup candidates. SOP_INDEX: 18-02..18-07 rows updated to new files; 18-08 row added; total 120→122; last_updated 2026-06-28. All test_status: untested (runtime verification deferred to staging). | Done | Claude (Sonnet) |
| 2026-06-21 | Client Portal v2.0 (logged-in client portal, mig 103 — `/api/portal/app/:projectId/*` + `/api/portal/admin/v2/:projectId/*`) | 11-10, 11-11, 11-12, 11-13 (new), SOP_INDEX | CLIENT PORTAL v2.0 SOP SET — 4 new SOPs written for the v2 logged-in portal (CLAUDE.md SOP law: every module build needs SOPs with a Section 14 Troubleshoot Agent Test Script). 11-10 Client login & invite (Admin invites → `POST /api/auth/invite` admin-only; client sets password → `project_client_users` membership upsert role=primary; `requirePortalAuth` JWT path scopes the client to their own project; documents the migration-103-not-applied dead-portal failure mode + supervisor-can't-invite 403). 11-11 My Actions & approvals (variation approve/decline via `/variations/:id/respond` login+write-gated, archives PDF + mirrors finance; meeting confirm/decline; payment-notify; selection redirect-not-inline; audit-before-respond). 11-12 Admin console (PortalV2Admin: `PATCH /settings` enable v2 + build_phase + team; milestones with is_current exclusivity + confidence/notes; selections; meetings + requestConfirmation; `POST /updates` with builder reasoning, always-publish, no notification; admin actions not audited). 11-13 Journey & documents (journey reads milestones+published updates+photos; documents archive re-checks client_visible on download; signed-URL vs un-expiring public_url; known photo 404 gap on legacy media endpoint). All four grounded in the v2 endpoints/tables from docs/portal_audit/CLIENT_PORTAL_FEATURE_REGISTER.md; full 14-section template; Section 14 has TC-01..TC-05 + 1–2 feature-specific cases tied to real endpoints. test_status: untested. SOP_INDEX total 116→120. | Done | Claude (Opus) |
| 2026-06-20 | Fee Proposals (Tender Manager) — auto-fill engine + dual-output | 03-01 fee_proposal_create (rewritten v2.0) | Fee proposal rebuilt: standardise on the Buildxact estimateitems export; auto-fill PC/PS from the Allowance flag, drop $0 categories, mirror the Buildxact quote №, floor area from COST METRIC, inclusions from the import (Builders Warranty pinned) blended with RFQ scope; relative week-by-week construction schedule auto-derived from `[task] SCHED` items (copies the Schedule module method, no commencement date). Dual-output: original template untouched + a new APB-Balanced template/transform (`proposalToApbDocxData`) with real company-profile content (ABN, MBA, USPs, 4-part warranty) + a placeholder guard. Commits 1efcce8 / efc9a61 / 23c7ab7. Reviewed via adversarial code review (1 high quote-№ collision + 4 mediums fixed) + an APB-knowledge critique (engine cleanup pass folded in). Section 14 expanded to TC-01..TC-12. APB DOCX merge-field spec + starter at docs/FEE_PROPOSAL_APB_TEMPLATE_SPEC.md / docs/fee-proposal-apb-template-starter.docx. | Done | Claude (Opus, dual adversarial review) |
| 2026-06-19 | Workforce Team Directory + Auth invite (employee↔login link) | 12-xx settings_invite_user (update pending) | App-login invites now take a ROLE (selector, not hardcoded supervisor) and link to the employee record (mig 100: invitations.employee_id, user_profiles.employee_id + one-login-per-employee). Dead /api/workforce/employees/:id/invite removed; audit-prompt + deployment-test docs updated. SOP body wording update is a follow-up. | Done (code) | Claude (Opus, plan+code review) |
| 2026-06-19 | Worker PWA + Operations/Carpentry (completion photos: site-media bucket, /api/worker/photos, signed-URL office viewers) | 10-03 add_completion_photo (NEW) | New SOP: any worker can attach a Storage-backed photo when completing a site task; office views it on the project/carpentry job. Plan adversarially reviewed before build; resubmit path-corruption + carpentry blind-spot must-fixes folded in. | Done | Claude (Opus, plan review) |
| 2026-06-19 | Worker PWA (iOS install: square PNG icons, apple meta on both entry HTMLs, PNG manifest icons, workbox mode=production, WorkerLayout safe-area padding) | 10-02 worker_pwa_ios_install (NEW) | New SOP for installing the worker app to the iOS home screen. Plan adversarially reviewed before build; must-fix folded in (standalone header was clipping under the notch). | Done | Claude (Opus, plan review) |
| 2026-06-17 | RFQ Engine (wizard Step 4) | 04-05_send_rfq | EDIT-LOCK + PER-EMAIL SEND — follow-up bugfixes from a 21-agent adversarial review of the shipped code (RfqEngine.jsx). (HIGH) mergePreservingEdits no longer resurrects a deselected trade's edited draft — the tail re-append now keeps only SENT rows, so an edited-but-unsent row whose trade/recipient was deselected on Step 3 is correctly dropped (was being silently re-included and emailed). (MEDIUM x3) sendOneRow now inspects the server's `skipped:true` idempotency response: on a retry where the email already went out it deletes the redundant freshly-queued rfq, skips a duplicate correspondence insert, and marks the row sent (rfqId null) — eliminating duplicate 'sent' rfq rows + spurious correspondence on lost-response retries and mid-send reloads. (MEDIUM) Blueprint QC "Use draft" (applyRfqDraft) now no-ops on a sent row + QC button disabled when sent — sent drafts are truly immutable. (MEDIUM) Restored the dropped Dropbox-link guard: if a draft still carries the literal "[add Dropbox link]" placeholder, inject the now-available folder URL, else warn instead of silently emailing a broken link. (LOW) A reload after the last row sent but before the package built now arms the all-sent effect on restore so the package finishes building. (LOW/perf) ensureJobContext caches on context existence (not URL truthiness) so a 28-trade empty-URL batch doesn't re-POST ensure-job-folders 28x. Refuted by the review (no change needed): duplicate-job race is closed, finalize stale-closure / reset-mid-batch fears. Deferred (low, data-hygiene): orphan 'tendering' job row on a failed first send; server serverLogged should be gated on !cErr. esbuild full-app bundle clean (0 warnings); frontend-only. | Done | Claude |
| 2026-06-17 | RFQ Engine (wizard Step 4) | 04-05_send_rfq | EDIT-LOCK + PER-EMAIL SEND for the RFQ Engine wizard (`src/pages/RfqEngine.jsx`). Fixes the live-job bug where Step-4 draft edits reverted on navigate-away/recompose and where leaving the page bounced the user to Step 3. (1) Hardened `mergePreservingEdits` so edited OR sent rows win by key even when the fresh row is blocked or dropped; added `mergeKeepingSent` for the Regenerate (force) path so sent rows stay immutable. (2) Edit-lock: the auto-rebuild effect early-returns on `activeStep===4` and `rebuildOutbound` self-guards via `activeStepRef`/`outboundRef` so no dep change / async subs load / settings-apply / remount can clobber a draft. (3) Deterministic restore: persist `highestStep` (session `version: 2→3`) and land on it; sanitize transient `sending` flags on restore. (4) New per-row **Send this RFQ** button (`sendOneRow`) with single-flight job/Dropbox `ensureJobContext`, a `sendInFlightRef` serialization guard (no duplicate-job race), queued-RFQ rollback on failure, sent-row lock (read-only subject/body + ✓ Sent badge). (5) Batch button → **Send remaining N**; package built once via an all-sent effect (`finalizeAllSentPackage`) whether finished per-row or batched. Server `/api/rfq/send` unchanged (already idempotent). SOP 04-05 Section 13 + TC-04..TC-08 added; test_status → untested. esbuild JSX parse-check OK (full eslint/vite build slow in sandbox; verified by parse + careful review). Implemented from a 5-agent diagnose→plan→adversarial-verify workflow; all 3 blocking + 2 should-fix defects from the verdict folded in. | Done | Claude |
| 2026-06-16 | RFQ Engine | 04-05_send_rfq | RFQ email rewritten for higher subcontractor reply rates. `composeRfqEmail` (src/lib/rfqComposer.js): subject reordered to lead with the trade, then address, then price-by date (`<Trade> quote — <addr> (price by <deadline>)`) for inbox scanning; the core ask (lump sum ex GST + deadline + "just reply to this email") moved to a single line directly under the opener instead of buried at the bottom; added a warm one-line opener; added an explicit bid/no-bid line near the end (quick "not this time" reply, kept top of list for next job) — a known reply-rate lever; closing CTA softened to "just reply here or give me a call". Scope/optional sections, HTML logo variant, and MAX_BODY_LINES trim logic unchanged. SOP 04-05 Section 6 updated to describe the new structure. node --check + functional render verified (eslint/full build skipped — sandbox node_modules read error, not code). | Done | Claude |
| 2026-06-16 | Procurement (Operations) | 16-07 through 16-10 (new), SOP_INDEX | BQ-10 P2/P3 build — Procurement Intelligence completed to the full A–T plan. New SOPs: 16-07 (Manage Suppliers & Performance — learning loop), 16-08 (Board/Calendar/Long-Lead views), 16-09 (AI Drafts — draft-only, never sends/orders), 16-10 (Draft a Purchase Order). Backed by migration 092 (supplier perf cols, item lifecycle timestamps, supplier_lead_observations ledger), procurementLearningService.mjs + procurementAiService.mjs, new procurementRoutes endpoints (suppliers perf, missing-items, quote-variance, backup-suppliers, long-lead, by-supplier, AI drafts, real draft-PO), ProcurementExtras.jsx tabs. Also applied 6 P0/P1 security+correctness fixes from a 7-agent assessment (role gates on all procurement reads, cost-field admin gate, committed_cost gating, stop writing frozen schedule_tasks.procurement_order_by, committed=approved-only). Section 14 (TC-01..TC-06) on each new SOP. SOP_INDEX total 112→116. Build + lint clean; live dedicated-test-job exercise 10/10 on production Supabase with clean teardown. | Done | Claude |
| 2026-06-16 | Procurement (Operations) | 16-01 through 16-06 (new), SOP_INDEX | BQ-10 PROCUREMENT INTELLIGENCE MODULE built (P1). DB: migration 085_procurement_intelligence.sql (suppliers, procurement_templates, procurement_items with GENERATED order_by_date + idempotency key job_id+source+source_ref; backfill from schedule_tasks.procurement_* which are now frozen; suppliers seeded from supplier_trade_defaults) + migration 091_procurement_template_seed.sql (62-item master template). Server: procurementService.mjs (generateProcurementPlan upsert, recomputeItemRisk, refreshJobRisk, computeCommittedCost), procurementRoutes.mjs (generate, items CRUD, command-centre, selections/blockers, request-quote, draft-po 501 stub, suppliers CRUD), registered in dev-api.mjs. Lock hook in financeCCRoutes.mjs auto-generates on financial_locked; committed_cost surfaced into FCC command-centre payload + Committed KPI. Schedule ripple (scheduleRoutes.mjs cascadeScheduleForward) syncs linked items' required_on_site_date so order_by recomputes. Frontend: Procurement.jsx (Command Centre / Register / Selections tabs) at /operations/procurement, Operations nav link, constants.js procurement enums. 6 SOPs written with Section 14 (TC-01..TC-06). SOP_INDEX total 106→112. Build + lint clean; risk math unit-tested 13/13; migrations 085/091 verified on scratch DB. | Done | Claude |
| 2026-05-30 | Carpentry Subsidiary | 15-01 through 15-06, SOP_INDEX | CARPENTRY SCHEMA CORRECTIONS (migration 066) — project_type enum expanded: 'both' replaced with 'full_package', added 'lockup'; status enum: added 'defects'; closeout_data JSONB column dropped from carpentry_jobs; new carpentry_job_performance table (final_revenue, final_labour_cost, final_material_cost, final_total_cost, labour_hours, final_margin_pct, budget_margin_pct, variance_pct, floor_area_m2, hours_per_m2, cost_per_m2, duration_days, timesheet_count, cost_entry_count, lessons_learned, closed_at, closed_by). Code: carpentryRoutes.mjs — JOB_STATUSES + PROJECT_TYPES updated; defaultMilestones() expanded to 4 templates (frame=10, fitoff=7, lockup=8, full_package=14, other=2); GET /api/carpentry/jobs/:id now joins carpentry_job_performance for complete jobs; closeout endpoint rewrites to performance table via upsert, returns { job, performance }; lessonsLearned field accepted. constants.js — CARPENTRY_JOB_STATUSES/LABELS + CARPENTRY_PROJECT_TYPES/LABELS updated. CarpentryDashboard — defects badge (orange), status filter updated, StatsBanner counts live=(active+defects), default projectType=full_package. CarpentryJobDetail — STATUS_BADGE defects added; edit lock removed (warning banner only); OverviewTab accepts performance prop; CloseoutModal adds lessons learned textarea + reads from performance table; main component stores performance state. SOP 15-02 TC-01/TC-02/TC-03 updated to reflect new enums and milestone counts. Build clean. | Done | Claude |
| 2026-05-30 | Carpentry Subsidiary, Workforce | 15-06 (new), 10-01 (updated) | CARPENTRY MODULE SPRINT 3 — Workforce integration + Job Closeout. Server: PATCH /api/workforce/timesheets/:id/carpentry-job (assign carpentry job to timesheet; validates not-approved guard); POST /api/carpentry/jobs/:id/closeout (compute final summary, set status=complete, write closeout_data JSONB snapshot). DB: carpentry_jobs.closeout_data jsonb column added to migration 065. Frontend: Workforce.jsx Approvals tab — expanded row shows carpentry job attribution dropdown (fetches active jobs, PATCH on change, seeded from existing carpentry_job_id); History tab — Project column shows carpentry job reference if set. CarpentryJobDetail.jsx — CloseoutModal component (summary stats + Confirm Close button); OverviewTab locked when status=complete/cancelled (Edit + Change Status replaced by status badge); Closeout Performance card shows final snapshot after close. SOPs: 15-06 (new — Close a Carpentry Job, TC-01 through TC-08); 10-01 updated to v1.1 (carpentry attribution step + TC-08/TC-09). SOP_INDEX total 105→106. Build clean. | Done | Claude |
| 2026-05-30 | Carpentry Subsidiary | 15-01 through 15-05 (new) | CARPENTRY MODULE SPRINT 1 — New module built. DB migration 065_carpentry_module.sql creates carpentry_jobs, carpentry_job_milestones, carpentry_job_costs, carpentry_site_diary tables + CJB sequence + alters timesheets/marketing_content_items/marketing_media_assets with carpentry_job_id FK. Server: carpentryRoutes.mjs registered in dev-api.mjs with full CRUD for jobs, milestones, diary, costs, and summary endpoint. Frontend: CarpentryDashboard.jsx (job list + stats + New Job modal with Buildexact lookup), CarpentryJobDetail.jsx (Overview/Schedule/Diary/Costs tabs). Nav: carpentry department + hammer icon added to AppShell.jsx BASE_DEPARTMENTS and ICONS. Routes /carpentry and /carpentry/:jobId added to App.jsx. Constants: CARPENTRY_JOB_STATUSES, CARPENTRY_PROJECT_TYPES, CARPENTRY_COST_TYPES, CARPENTRY_MILESTONE_STATUSES added to constants.js. 5 SOPs written: 15-01 (overview), 15-02 (create job), 15-03 (milestones), 15-04 (site diary), 15-05 (costs). All include Section 14 test scripts. Build passes. SOP_INDEX.md total updated to 105. | Done | Claude |
| 2026-05-30 | Marketing Intelligence | 19-01_intelligence_dashboard.md | P1.8 + P2.2–P2.4 + P2.8 COMPLETE — Added GA4 sync (POST /api/intelligence/sync/ga4 — GA4 Data API v1beta, source/medium + top pages → ga4_snapshots), GBP sync (POST /api/intelligence/sync/gbp — Performance API, calls/clicks/directions → gbp_snapshots), shared getGoogleAccessToken() helper reusing GOOGLE_DRIVE_* OAuth creds. Dashboard endpoint extended: suburb_engagement (deterministic leads-by-suburb aggregation, last 90 days) and ai_summary (Haiku, module-level Map cache keyed by ISO week). UI (MarketingIntelligence.jsx): AiSummary banner, SuburbEngagement bar chart, WebsitePages table (live fetch from /api/intelligence/pages, url_path/type/status/cluster/position/impressions/stale ⚠/Generate Brief button with inline brief display), four sync buttons (Social/GSC/GA4/Google Business). SOP 19-01 updated to v1.1 with 7 new test cases (TC-08 suburb engagement, TC-09 AI summary, TC-10 website pages, TC-11 brief inline, TC-12 GA4 sync, TC-13 GBP sync, TC-14 sync disabled during active sync). All committed in e709a13. | Done | Claude |
| 2026-05-30 | Client Portal | 11-03, 11-04, 11-05, 11-06, 11-07, 11-08; portalRoutes.mjs; module4Routes.mjs | TIER 2 FIX IMPLEMENTATION — all P0–P2 findings from AUDIT_TIER2_2026-05-30.md implemented. SOPs: (CRIT-T2-001) 11-03 corrected to use `headline/body/weekOf` not `title/summary`; (CRIT-T2-002) 11-05 decision respond corrected to `action: approve/decline` + `clientNote` not `response/note`; (CRIT-T2-003) 11-06 variation completely rewritten to use `portal_decisions` with `type:"variation"` — not `portal_claims`; (CRIT-T2-004) 11-07 messages corrected to use `body` not `message` for both admin and client; (BUG-T2-005) 11-04 photos corrected from multipart/Supabase to JSON base64/Dropbox; (BUG-T2-006) 11-05 decision create — `type` added as required field; (BUG-T2-007) 11-08 milestones corrected to `key/label/eta/achievedAt`. Code: (STDS) portalRoutes.mjs — all 11 bare-row/missing-ok responses wrapped with `ok: true` + entity keys (generate-token, updates POST/PATCH, photos POST/upload, milestones, decisions, claims, site-walks, builder-messages, summary GET); (STDS) module4Routes.mjs — hardcoded `subtotal * 0.1` replaced with `gstAmount(subtotal)` using local `GST_RATE` constant. All 6 failing SOPs now `test_status: static_pass`. Lint + build clean. | Done | Claude |
| 2026-05-30 | Client Portal, Tendering, Operations, WHS | 25 SOPs (03-01–03-04, 05-01–05-06, 08-01–08-06, 11-01–11-09) | TIER 2 ADVERSARIAL AUDIT — static code analysis of all 25 Tier 2 SOPs against live server routes. 4 critical bugs found: CRIT-T2-001 portal_add_weekly_update uses wrong body fields (title/summary vs headline/body; weekOf required but absent); CRIT-T2-002 portal decision respond uses wrong field (response vs action) and wrong values (approved/rejected vs approve/decline); CRIT-T2-003 portal_variation documents wrong endpoint (portal/admin/claims with description vs portal/admin/decisions with type=variation); CRIT-T2-004 portal_send_message uses wrong field (message vs body for both admin and client). 3 medium bugs: BUG-T2-005 photos upload documents multipart/Supabase but code uses JSON base64/Dropbox; BUG-T2-006 decision create missing required `type` field; BUG-T2-007 milestones uses wrong fields (name/targetDate/completed vs label/eta/achievedAt; key required). 12 API standards violations (10 bare-row responses in portalRoutes, hardcoded GST 0.1 in module4Routes). Auth confirmed OK — app.use middleware covers all /api/portal/admin/*. 18 SOPs PASS, 7 FAIL (all in portal module). 109 TCs PASS, 17 FAIL, 19 SKIP. Full report at docs/sops/AUDIT_TIER2_2026-05-30.md. All 25 SOP test_status fields updated. | Done | Claude |
| 2026-05-30 | All modules | 34 new SOPs (05-01–05-06, 07-01–07-03, 08-01–08-06, 10-01, 11-01–11-09, 12-01–12-06, 13-01–13-03, 14-01–14-02) | SOP FULL BACKFILL COMPLETE — 34 SOPs written from scratch covering all remaining modules. Operations: dashboard overview (05-01), open project (05-02), issue PO (05-03), Buildexact link (05-04), global Gantt (05-05), trade conflicts (05-06). Site Diary: write entry (07-01), voice capture (07-02), view entries (07-03). WHS: upload compliance (08-01), check status (08-02), induction setup (08-03), complete induction (08-04), log incident (08-05), resolve incident (08-06). Workforce: overview (10-01). Client Portal: enable (11-01), view-as-client (11-02), weekly update (11-03), photos (11-04), decisions (11-05), variation (11-06), messages (11-07), milestones (11-08), client guide (11-09). Settings: company details (12-01), invite user (12-02), manage users (12-03), Buildexact (12-04), Xero (12-05), Dropbox (12-06). Subcontractors: add (13-01), import CSV (13-02), manage (13-03). Cost Intelligence: view benchmarks (14-01), pre-tender estimate (14-02). All include Section 14 test scripts. SOP_INDEX.md total corrected from 105 to 100. All Tier 2 + Tier 3 SOP backfill tasks complete. | Done | Claude |
| 2026-05-30 | Finance, RFQ Engine, Sales | TIER 1 FIX IMPLEMENTATION — all P0–P2 findings from AUDIT_TIER1_2026-05-30.md implemented. Code: (CRIT-001) requireAuth added to all 12 rfqPackageRoutes; (CRIT-002) rfqs.status 'accepted' mirror added to quote acceptance handler; (CRIT-003) POST /api/finance/documents/:id/hold endpoint created; (BUG-004) STAGE_PROB corrected to APB values; (BUG-005) duplicate command-centre route with bad column removed; (BUG-006) dead WIPAA 307 redirect removed; (BUG-007 + STDS-001) rfqPackageRoutes fully rewritten with ok/err/rowsToCamel; (STDS-002) translateDbError applied throughout financeRoutes and financeCCRoutes. SOP docs: (LOW-001) 04-05 partial-send scope status rule documented; (LOW-002) 04-09 addendum eligible statuses corrected to sent/reminded/followed_up; (LOW-003) finance_put_invoice_on_hold API URL corrected to POST /api/finance/documents/:id/hold. Commit: c2cbd4a. | Done | Claude |
| 2026-05-30 | Tendering, Operations, WHS, Client Portal | Tier 2 audit | TIER 2 AUDIT PROMPT — wrote docs/sops/AUDIT_TIER2_PROMPT.md, a self-contained adversarial troubleshoot-agent prompt to audit all 25 Tier 2 SOPs against live code. Lists exact backing route files per module and a "high-suspicion" checklist: (1) auth coverage on /api/portal/admin/* routes (same risk class as the RFQ no-auth finding); (2) portal admin endpoints returning bare rowToCamel rows vs { ok }; (3) specific Portal field-name drift to verify against portalRoutes.mjs (updates weekOf/headline/body, decision respond action vs response, messages body vs message, claims stageName vs description); (4) PO/Buildexact/tender specifics; (5) WHS compliance status thresholds + induction required fields. Targets AUDIT_TIER2_2026-05-30.md. | Done | Claude |
| 2026-05-30 | Tendering, Operations, WHS, Client Portal | 25 SOPs (03-01–03-04, 05-01–05-06, 08-01–08-06, 11-01–11-09) | SOP TIER 2 BACKFILL — 25 SOPs written from scratch with full test scripts, each grounded in the actual server routes. Tendering: fee proposal create/send (module5Routes), tender board (reads jobs+rfqs), archive/delete (tender/job-delete cascade). Operations: dashboard (operations/projects health), open project (projects/:id/trades + trade-communication/respond), issue PO (po/issue), Buildexact link (buildexact/job/:id), global Gantt (operations/global-tasks), trade conflicts (operations/trade-conflicts). WHS: upload/check compliance (whs/compliance, computed_status from expiry), induction QR setup + complete (induction/:projectId/info+submit), log/resolve incident (whs/:projectId/reports + report/:id). Portal: enable (generate-token), view-as-client, weekly update, photos, decisions (+ client respond), variation, messages, milestones (upsert), client guide. A Tier 2 adversarial audit (AUDIT_TIER1_PROMPT.md template) is recommended next — expect to catch the same class of doc-vs-code drift the Tier 1 audit found. | Done | Claude |
| 2026-05-30 | All modules | Tier 1 audit | TIER 1 AUDIT PROMPT — wrote docs/sops/AUDIT_TIER1_PROMPT.md, a self-contained adversarial troubleshoot-agent prompt to audit all 36 Tier 1 SOPs (Finance, Sales, RFQ, Schedule) against live code. Targets the camelCase/snake_case, wrong-nesting, and non-existent-column bug classes found in the Marketing audit. Drove the Tier 1 audit run below. | Done | Claude |
| 2026-05-30 | Finance, Sales, RFQ Engine, Scheduling | 36 SOPs (09-01–09-12, 02-01–02-07, 04-01–04-09, 06-01–06-08) | TIER 1 ADVERSARIAL AUDIT — static code analysis of all 36 SOPs against live server routes. 3 critical bugs found: CRIT-001 rfqPackageRoutes.mjs has NO requireAuth on any route (security); CRIT-002 rfqs.status never mirrors 'accepted' on quote acceptance (SOP 04-08 TC-01); CRIT-003 hold invoice endpoint does not exist and URL prefix is wrong (SOP 09-05 all TCs fail). 4 medium bugs: BUG-004 STAGE_PROB values in salesRoutes.mjs don't match SOP 02-04 docs; BUG-005 duplicate command-centre route with non-existent column; BUG-006 WIPAA endpoint 307 redirect with dead code; BUG-007 RFQ routes return raw snake_case (no rowsToCamel). 2 API standards violations (rfqPackageRoutes: no ok/err/rowsToCamel; financeRoutes: raw DB errors exposed). 141 TCs confirmed PASS, 7 FAIL, 25 SKIP. All 36 SOP test_status fields updated to static_pass or static_fail. Full report at docs/sops/AUDIT_TIER1_2026-05-30.md | Done | Claude |
| 2026-05-30 | Getting Started, Global Navigation | 00-01, 00-02, 00-03, 01-01, 01-02 | TEST SCRIPTS ADDED — Section 14 (Troubleshoot Agent Test Script) added to all 5 Getting Started and Navigation SOPs. Updated frontmatter: last_reviewed → 2026-05-30, test_status: untested added. TC-01 through TC-05 for each: login happy/error paths, signup validation, invitation acceptance, role-based navigation, project selector context persistence. |
| 2026-05-30 | All modules | SOP_INDEX.md | INDEX AUDIT AND REPAIR — SOP_INDEX.md had stale planned filenames for Sales (02), RFQ Engine (04), and Schedule (06) modules. All 23 file paths updated to match actual filenames on disk (nn-nn_topicname.md format). Sales 02-07 topic corrected from "Archive or delete a lead" to "View and manage conversation history" (which is the SOP that was actually built). RFQ 04-01 through 04-09 topics corrected to match actual built SOP content. Schedule 06-01 through 06-08 topics corrected to match actual built content (overview, AI generation, baseline lock, EOT, dependency map, drag/resize, ripple cascade). All 105 SOPs verified to have test scripts (Troubleshoot Agent Test Script section). | Done | Claude |
| 2026-05-30 | Sales, Scheduling, RFQ Engine | 02-02 through 02-07, 04-01 through 04-09, 06-01 through 06-08 | SOP TIER 1 BACKFILL — 23 SOPs written from scratch with Section 14 test scripts. Sales: move lead (02-02), log activity (02-03), qualifying score (02-04), Blueprint Insight (02-05), transcript analysis (02-06), conversation history (02-07). RFQ Engine: overview (04-01), create package (04-02), scope extraction (04-03), trade packages (04-04), send RFQ (04-05), receive quotes (04-06), quote comparison (04-07), accept quote (04-08), addendum (04-09). Schedule: overview (06-01), generate schedule (06-02), edit tasks (06-03), baseline lock (06-04), EOT (06-05), dependency map (06-06), drag/resize (06-07), ripple cascade (06-08). SOP_INDEX.md updated — all 23 file paths corrected to new naming convention (nn-nn_name.md format). Total count corrected from 105 to 100. | Done | Claude |
| 2026-05-29 | Marketing + Marketing Intelligence | 18-01 through 18-07, 19-01 through 19-08 | ADVERSARIAL AUDIT — all 15 SOPs tested against live code. 3 critical bugs found: CRIT-01 publish endpoint camelCase mismatch (ContentLibrary.jsx sends contentItemId, server reads content_item_id → HTTP 400 on every publish), CRIT-02 intelligence dashboard reads wrong API key paths (data?.thisMonth vs data?.dashboard?.this_month), CRIT-03 content-performance query selects non-existent columns (mode, content_pillar, engagement_rate). 5 API standards violations in marketingRoutes.mjs (no ok()/err() import, no rowsToCamel, raw Supabase errors). 5 banned phrases missing from automated checker (luxurious, stunning, bespoke, curated, elevated). All test_status fields updated in SOP frontmatter. Full report at docs/sops/AUDIT_MARKETING_2026-05-29.md | Done | Claude |
| 2026-05-29 | Finance | 09-01 through 09-12 | MODULE 1 Finance SOPs written — all 12 Finance SOPs created including 09-01 (Upload Invoice), 09-02 (AI Extraction Review), 09-03 (Job Match), 09-04 (Approve), 09-05 (Hold), 09-06 (Reject), 09-07 (Job Command Centre Overview), 09-08 (Progress Claims), 09-09 (Margin Risk Portfolio), 09-10 (Variations), 09-11 (WIPAA Review), 09-12 (Cashflow Forecast). All include Section 14 test scripts tied to actual API routes. SOP_INDEX.md updated — 09-10 filename corrected, 09-11 and 09-12 added, total updated to 105. | Done | Claude |
| 2026-05-29 | Marketing Intelligence | 19-01 through 19-08 | MODULE 5 Marketing Intelligence built — updated all 8 SOPs from "planned — not yet built" to "built 2026-05-29". Section 14 test scripts existed pre-build and are ready to run on deployment. SOP_INDEX.md updated — Section 14 column set to Yes for all 8. | Done | Claude |
| 2026-05-20 | All modules | All SOPs | Initial SOP inventory created (82 SOPs, all Draft) | Draft | Sam Morris |

---

*Add new rows at the top when changes are made.*  
*Format: YYYY-MM-DD | Section | SOP file name | Description | Done/In Progress/Pending | Name*
