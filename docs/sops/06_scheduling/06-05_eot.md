---
sop_version: 1.1
last_reviewed: 2026-07-02
app_version: 1.0 — built
screenshot_status: not_applicable
owner: Admin
test_status: static_pass
---

# SOP 06-05: Extension of Time (EOT) — Raise, Approve, and Apply

**Module:** Operations → Schedule Manager → Delays tab  
**SOP ID:** 06-05  
**Status:** Draft  
**Priority:** High

---

## 1. Who uses this
Admin (raises and approves EOTs)

## 2. When to use it
When a delay event occurs that is not the builder's fault and justifies extending the contract practical completion date. Common EOT reasons:
- Client-instructed variations that add time to the programme
- Inclement weather beyond what was reasonably anticipated
- Delays in client approvals or documentation
- Delays caused by third parties (council, utilities)

EOTs must be formally raised and approved — they are contractual. Do not adjust task dates manually to cover delays without raising an EOT.

## 3. What this does
Creates a formal EOT claim record with a reason code, description, and number of days claimed. The EOT goes through a simple approval workflow (pending → approved/rejected). Once approved, the EOT can be applied to the schedule — which shifts ALL task dates forward by the approved number of days.

The EOT reason codes accepted: `inclement_weather`, `client_variation`, `client_delay`, `third_party`, `supply_delay`, `other`

## 4. Before you start
- The project must have a generated schedule with tasks
- Note the current practical completion date (last task `end_date`) so you can verify the shift after applying
- Have the delay event details ready: reason, description, and the number of calendar days being claimed

## 5. Step-by-step process

**Raising an EOT:**
1. Navigate to Operations → Schedule → **Delays** tab
2. Click **+ Raise EOT**
3. Fill in:
   - **Reason code** — select from the dropdown (required)
   - **Description** — plain English explanation of the delay event (e.g. "3 weeks of rain in June prevented concrete pours and frame erection")
   - **Days claimed** — the number of calendar days extension requested (required)
4. Click **Raise** — EOT is created with `status = 'pending'`

**Approving or rejecting an EOT:**
1. On the Delays tab, find the EOT with status "Pending"
2. Click the EOT to review it
3. To approve: enter the **Days approved** (may differ from days claimed), click **Approve**
4. To reject: click **Reject**
5. Status updates to `approved` or `rejected`

**Applying an approved EOT to the schedule:**
1. Find the approved EOT on the Delays tab
2. Click **Apply to schedule**
3. Confirm — this shifts every task's start and end date forward by `days_approved` calendar days
4. The practical completion date extends accordingly
5. EOT `applied_at` is recorded — the EOT cannot be applied again

## 6. What happens next
When an approved EOT is applied:
- Every `schedule_tasks` record for the project has its `start_date` and `end_date` shifted forward by `days_approved`
- The `schedule_eot.applied_at` is set to now
- The EOT cannot be applied again (one-time operation)
- The baseline dates (if locked) remain unchanged — the drift will now correctly show the EOT-caused shift as a gap between ghost bars and current bars

## 7. Common mistakes

| Mistake | Why it happens | How to avoid it |
|---------|---------------|-----------------|
| Applying an EOT twice | Clicking Apply again thinking it didn't work | Applied EOTs cannot be applied again — check the Delays tab for `applied_at` |
| Setting days_approved higher than days_claimed without justification | Rounding up | Approved days should be ≤ claimed days unless there is a documented reason |
| Adjusting task dates manually instead of raising an EOT | Quicker in the short term | Manual date adjustments don't create a contractual record. Always raise a formal EOT for legitimate delay events. |

## 8. Troubleshooting

| Problem | Solution |
|---------|----------|
| "EOT must be approved before applying" error | The EOT is still in pending status — approve it first |
| "No approved days set" error | Enter `days_approved` when approving |
| Apply button disabled | Check if the EOT has already been applied (`applied_at` is set in the Delays tab) |

## 9. Related modules
- [Schedule Manager overview](06-01_schedule_overview.md) — SOP 06-01
- [Edit a schedule](06-03_edit_schedule.md) — SOP 06-03 (manual task edits vs EOT)
- [Lock a baseline](06-04_baseline_lock.md) — SOP 06-04 (baseline stays fixed when EOT applied)

## 10. Screenshot placeholders
- [ ] Delays tab with at least one EOT in the list (showing status badges)
- [ ] "+ Raise EOT" modal with all fields visible
- [ ] Approved EOT with "Apply to schedule" button
- [ ] Delays tab after applying — showing `applied_at` timestamp

## 11. Automation notes
- Raise: `POST /api/schedule/:projectId/eot` with `{ reason_code, description, days_claimed }` → creates record with `status = 'pending'`
- Approve/reject: `PATCH /api/schedule/:projectId/eot/:eotId` with `{ status: 'approved', days_approved }` or `{ status: 'rejected' }`
- Apply: `POST /api/schedule/:projectId/eot/:eotId/apply` — loops all active tasks, adds `days_approved` to both `start_date` and `end_date`, sets `applied_at`
- Validation: apply endpoint returns 400 if `status !== 'approved'` or `days_approved` is null/zero

## 12. Edge cases and limits
- A rejected EOT cannot be approved later — raise a new EOT if the rejection was in error
- An applied EOT cannot be unapplied — if the shift was wrong, you must manually adjust task dates or raise a corrective edit
- If the baseline is locked, applying an EOT shifts task dates but leaves ghost bars at the pre-EOT positions — this drift is intentional and reflects the EOT impact
- The `days_claimed` and `days_approved` fields accept whole numbers only; fractional days are not supported

## 13. Owner of the process
Admin  
Next review: 2026-12-02

---

## 14. Troubleshoot Agent Test Script

### Pre-test setup
- [ ] A project with a generated schedule (at least 5 tasks, with known start/end dates)
- [ ] Note the current PC date (last task `end_date`)

### Test cases

**TC-01 — Raise an EOT**
1. Navigate to Schedule → Delays tab
2. Click + Raise EOT
3. Fill: reason_code = "inclement_weather", description = "Heavy rain for 3 weeks", days_claimed = 14
4. Click Raise
5. Expected: EOT appears in the Delays list with `status = 'pending'`
6. Expected DB: `schedule_eot` row with correct fields and `status = 'pending'`
- [ ] Pass  [ ] Fail

**TC-02 — Required fields validation**
1. Click + Raise EOT
2. Leave reason_code blank, enter days_claimed = 7
3. Click Raise
4. Expected: validation error shown — "reason_code and days_claimed required" (or similar)
- [ ] Pass  [ ] Fail

**TC-03 — Approve an EOT**
1. Click the pending EOT from TC-01
2. Enter days_approved = 14
3. Click Approve
4. Expected: status changes to "approved"
5. Expected DB: `status = 'approved'`, `days_approved = 14`, `resolved_at` set
- [ ] Pass  [ ] Fail

**TC-04 — Apply EOT to schedule**
1. Note the `start_date` and `end_date` of 3 specific tasks before applying
2. Click Apply to schedule on the approved EOT from TC-03
3. Confirm
4. Expected: all active tasks shifted forward by 14 days
5. Expected DB: all active tasks have `start_date` and `end_date` each increased by 14 calendar days
6. Expected DB: `schedule_eot.applied_at` is now set
7. Expected: PC date (last task `end_date`) is now 14 days later than before applying
- [ ] Pass  [ ] Fail

**TC-05 — Cannot apply an EOT twice**
1. Click Apply again on the already-applied EOT from TC-04
2. Expected: Apply button is disabled OR the server returns an error preventing re-application
- [ ] Pass  [ ] Fail

**Feature case — Rejected EOT cannot be applied**
1. Raise a second EOT (any reason code, days_claimed = 5)
2. Reject it (status = 'rejected')
3. Attempt to click Apply to schedule
4. Expected: Apply button is absent/disabled, or server returns "EOT must be approved before applying"
- [ ] Pass  [ ] Fail

### Post-test checklist
- [ ] EOT creates with pending status and all required fields
- [ ] Approval records days_approved and resolved_at
- [ ] Apply shifts all active task dates by the correct number of days
- [ ] Applied EOT cannot be re-applied
- [ ] Rejected EOT cannot be applied
- [ ] Update `test_status` in frontmatter
- [ ] Add entry to SOP_CHANGELOG.md
