# Dependencies
node_modules

# Builds
dist
*.local

# Env & secrets
.env
.env.*
!.env.sandbox.example
scripts/hardening-watch.env
!scripts/hardening-watch.env.example

# Logs
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor/OS
.DS_Store
.idea
.cursor/

# Vite cache
.cache
server/luts/*.cube

# Test output (local PDF samples etc.)
scripts/output/
.agent-test-context.local.md
.test-credentials.local
e2e/.runtime.json
.export-redacted-tmp/
e2e/report/
e2e/tests/**/__snapshots__/
test-results/
playwright-report/
playwright/.cache/
.claude/launch.json

# Hardening export archives + scratch (do not commit)
blue-leaf-hub-hardening-*.zip
_tmp_*.mjs

# Review/screenshot export artifacts (heavy binaries/bundles — keep source only)
*.zip
e2e/screenshots/
docs/ui-review/export-*/
docs/ui-redesign/
docs/ui-review/

# CI workflows — commit separately with a workflow-scoped token (Gate 10)
.github/workflows/
